# File Guide

Per-file map of the codebase: what each file does plus the invariants, gotchas, and design notes that matter when changing it. The compact one-line-per-file index lives in [CLAUDE.md](../CLAUDE.md#project-structure); this is the detailed version. Read a file's entry before modifying the file or adding a new caller of its exports; update the entry when adding, removing, or renaming a file or when its contract changes (and keep the CLAUDE.md one-liner in sync). Test files get entries only when the test setup itself carries invariants worth noting.

See [ARCHITECTURE.md](ARCHITECTURE.md) for the system-level design these files implement.

## cmd/lightjj/

- **`main.go`** — CLI entry point, flag parsing, embeds frontend-dist/
- **`session_file.go`** — Agent port-discovery: writes {pid,addr,repo_dir,mode} to `$XDG_RUNTIME_DIR/lightjj/sessions/<pid>.json` after bind; stale-pid sweep; symlink-race-hardened TempDir fallback (verifyOwnedDir double-check). No-op on Windows. Agents match by repo_dir instead of needing the random port.

## internal/jj/

Command builders + data models (PURE — no I/O, no side effects)

- **`commands.go`** — Functions that return []string args for jj subcommands
- **`commit.go`** — Commit model with ChangePrefix/CommitPrefix, Immutable, Divergent, WorkingCopies, Mine, AuthorEmail, Timestamp
- **`bookmark.go`** — Bookmark model + output parsers; ParseBookmarkListOutput/ParseRemoteListOutput take defaultRemote param for sort order
- **`alias.go`** — jj config alias parser
- **`file_change.go`** — FileChange model, FilesTemplate (single-call file stats + conflict info), ParseFilesTemplate
- **`divergence.go`** — DivergenceEntry, Divergence() template builder, ParseDivergence. committer_ts DELIBERATELY absent (structurally inverted for --at-op). splitNonEmpty helper.
- **`selected_revisions.go`** — Multi-revision selection helper
- **`version.go`** — Semver type + ParseSemver + named feature gates (WorkspaceRootTmpl etc)
- **`workspace_store.go`** — Protobuf parser for .jj/repo/workspace_store/index (name→path map). <0.40 fallback — ≥0.40 uses WorkspaceRef.root() in the WorkspaceList template.
- **`workspace_store_test.go`** — Parser tests with real binary data

## internal/runner/

CommandRunner interface + implementations

- **`runner.go`** — Interface definition (Run, RunWithInput, RunForMutation, StreamCombined, RunRaw, WriteFile)
- **`local.go`** — LocalRunner: exec("jj", args) with configurable Binary. WriteFile does symlink-escape hardening (EvalSymlinks on parent + Lstat leaf) before os.WriteFile.
- **`ssh.go`** — SSHRunner: wraps jj args in ssh command. WriteFile pipes content via `cd <repo> && cat > <path>` over stdin — no symlink check (same trust boundary as remote shell).

## internal/api/

HTTP handlers

- **`server.go`** — Route registration, runMutation, op-id caching, workspace store reader, helpers
- **`handlers.go`** — All endpoint implementations, flag validation
- **`handlers_test.go`** — Handler tests with MockRunner
- **`integration_test.go`** — Integration tests (build-tagged)
- **`watcher.go`** — fsnotify on .jj/repo/op_heads/heads/ + SSE push, periodic util snapshot. SSH mode uses `sshPollLoop` (`PollOpId` — implicit snapshot + op-id in one round trip; no remote deps; CAS against pre-poll cachedOp so a concurrent runMutation advance isn't regressed). `interval <= 0` gates both loops (parity). Stale-WC detection: both loops check `isStaleWCError()` → `setStale(bool)` (staleMu-serialized Swap+broadcast — handler clear can't race a loop set and emit sentinels out-of-order; Swap edges don't re-fire so the race has no self-heal) → `evStaleWC`/`evFreshWC` sentinels ("!"-prefixed on the existing `chan string`) → frontend non-dismissable warning with "Update stale" action. Both handleSnapshot + handleWorkspaceUpdateStale call `clearStale()`. `handleEvents` refreshes cachedOp if empty (read-only session path), emits current stale state unconditionally on connect (both branches — false branch covers SSE-dropped-during-staleness → CLI-recovered → reconnect), per-write WriteDeadline extension (not blanket disable — dead TCP surfaces in ≤85s).
- **`watcher_test.go`** — Broadcaster tests (subscribe/broadcast/drop), sshPollLoop tests via seqRunner (hook field for mid-call injection), setStale edge-only invariant, handleEvents refresh-if-empty table, CAS-guards-concurrent-advance
- **`tabs.go`** — TabManager: per-tab Server + Watcher mounted at /tab/{id}/; TabResolve + TabFactory injection (mode-agnostic validation via closure); openTabs persisted to config.json (tab 0 excluded — fights with -R on next launch); startup restore re-runs resolve() so moved repos log-and-skip
- **`config.go`** — Server-side JSONC config (tailscale/hujson); mergeAndWriteConfig applies per-key RFC-6902 add patches so comments on existing keys survive; fresh installs (ENOENT) seed from configTemplate, corrupt files NEVER reseed — 422 + frontend warning keeps user's bad file untouched (ErrConfigUnparseable sentinel). writePersistedTabs filter-merges openTabs through the same patch path. GET /api/config returns Standardized JSON (422 on corrupt); GET/POST /api/config/raw round-trips raw JSONC for ConfigModal (text/plain only, 1MB cap, isLocalOrigin guard).
- **`config_jsonc.go`** — hujson helpers: standardizeJSONC (bytes.Clone first — hujson.Standardize mutates input via aliased Extra slices), unmarshalJSONC, patchConfigKeys. The centralised "Standardize before Unmarshal" idiom — any new json.Unmarshal on config bytes must go through unmarshalJSONC or break the first time the user adds a comment.
- **`config_template.go`** — First-run JSONC template string (in-tree constant, not a file). Teaching comments over theme, fontSize, editorArgs, font families. Kept in sync with frontend defaults and [CONFIG.md](CONFIG.md) by convention — see the top-of-file comment.
- **`jsonstore.go`** — Generic readJSONStore[T]/atomicWriteJSON/upsertByID[T]/removeByID[T] over flat-file JSON arrays; shared by annotations + doc-comments
- **`annotations.go`** — CRUD for per-changeId review comments (annotations/{changeId}.json); changeId path-traversal validation via regex
- **`doc_comments.go`** — CRUD for doc-mode range-anchored comments/suggestions. Store key = sha256(RepoPath+"|"+cleanDocPath(filePath))[:16]. cleanDocPath: ToSlash+Clean+strip ./, reject ../ and / (400). POST stamps id/createdAt if omitted, validates anchor.selection, preserves existing resolution/resolvedAt/createdAt on upsert (agent re-POST can't wipe a human accept). DELETE requires id (no clear-all).
- **`agent_docs.go`** — `GET /api/agent` serves go:embed agent_api.md (the agent-facing contract). agent_docs_test.go fetches it and asserts every /api/* path mentioned is a registered route — the structural guard against doc/route drift.
- **`open.go`** — Open-in-$EDITOR: buildEditorArgv ({file}/{line} substitution, argv[0] validation), handleOpenFile. Setsid via build-tagged detachProcess (open_unix.go / open_windows.go)
- **`gzip.go`** — Gzip response middleware (lazy-init writer, Flush passthrough for SSE)

## internal/parser/

Graph log parser

- **`graph.go`** — Parses jj log graph output with _PREFIX: markers into GraphRow[]

## testutil/

Test infrastructure

- **`mock_runner.go`** — MockRunner with Expect(args)/Verify() pattern

## frontend/

Svelte 5 SPA (Vite + TypeScript + pnpm)

- **`src/AppShell.svelte`** — Tab-switch host. Snapshots `{selectedIndex, revsetFilter, activeView, diffScrollTop}` via `appRef.getState()` BEFORE the `{#key activeTabId}` remount, feeds as `initialState` prop to the new App instance. Inline modes NOT preserved (half-complete rebase across tabs is a footgun).
- **`src/App.svelte`** — Main app shell: layout, keyboard handling, state management, revset filter bar (owned here — direct state access, no callback props). `handleKeydown` delegates to `routeKeydown` (keyboard-gate.ts) with handler callbacks; gate ORDER is load-bearing (Cmd+K before isInInput = works in text fields; inlineNav swallows everything = normal-mode keys don't leak into modes). Cmd+K uses `closeModals()` not `closeAllModals()` — palette opens without cancelling inline modes.
- **`src/App.interactions.test.ts`** — In-process keyboard-gate tests (j/k, Space, R-mode swallows globalKeys, branches fallthrough, `}` no-annotations, agent-navigate gate). Mounts App with mock-api; presses on document.body (bubbles to `<svelte:window>`).
- **`src/testutil/mock-api.ts`** — `vi.mock('./lib/api')` netStubs: Proxy-based `mockApi` (records calls, routes reads to Fixtures), `mkRevision`/`mkInfo` builders, `triggerNavigate` to fire the onNavigate handler. importActual keeps pure helpers (effectiveId etc.) real.
- **`src/testutil/wait-for.ts`** — `waitForFrame()` (double-rAF), `waitFor(pred)` polling
- **`vite.config.ts`** — Dev proxy + build output to ../cmd/lightjj/frontend-dist/

## frontend/src/lib/

- **`api.ts`** — Typed API client, op-id tracking, commit_id-keyed LRU cache. `wireAutoRefresh` SSE reconnects on backend restart via `everSawEvent` heuristic (mirrors Go's `everSawLine`): handleEvents pushes op-id immediately on connect, so CLOSED-before-event = watcher absent → give up; CLOSED-after-event = transient → backoff retry.
- **`RevisionGraph.svelte`** — Revision list with graph gutter rendering; JS-tracked hoveredIndex (mousemove-driven, not :hover). Virtualized via `createWindower` (virtual.svelte.ts) above VIRTUALIZE_THRESHOLD=150 flatLines (~50 commits). Fixed 18px rows → pure-arithmetic windowing (no per-row measurement). Row template is a {#snippet} shared by virtual/eager paths. Pure display — no revset filter input (that lives in App.svelte above RevisionGraph inside `revision-panel-wrapper`).
- **`virtual.svelte.ts`** — `createWindower()` fixed-row-height virtualization. count/scrollEl passed as getters → reactive without $effect glue. `items` is $derived reading count directly → no lag-frame between count shrink and item re-render (the tanstack setOptions-via-$effect problem). `scrollToIndex` implements align:'auto'. `holdViewport(scrollEl, anchorSel, fn, gen?)` runs a layout-shifting mutation without the line under the user's eye moving: capture first `anchorSel` match with top ≥ container-top → fn() → `await tick()` → correct scrollTop by delta (tick = post-DOM-flush pre-paint; rAF would paint shifted first). `anchorSel` MUST be per-row (e.g. `.diff-file`) — a wrapper div's top doesn't move when its contents reflow. gen() lets a concurrent scrollTo win.
- **`GraphSvg.svelte`** — SVG renderer for graph gutter characters (pipes, curves, node dots)
- **`DiffPanel.svelte`** — Diff viewer: unified/split toggle, syntax highlighting, edit-state management. Collapse is a DERIVED per-file decision (`isFileCollapsed`: userExpanded > userCollapsed > auto predicate, evaluated at render time — there is no post-render auto-collapse effect, so big files never transiently build their bodies); offscreen bodies defer behind estimated-height placeholders mounted by an IntersectionObserver (±2 viewports, one per frame, `holdViewport` re-anchoring) or by `revealFile(path)` — the single force-mount/expand hook every programmatic jump (search, hunk/annotation nav, editor/preview open, file-tab click) must use. Compute caps (HIGHLIGHT_SKIP_LINE_LIMIT/WORD_DIFF_LINE_LIMIT, 5k lines) are independent of the collapse thresholds. Reset effect (activeRevisionId change) clears edit/search/expanded state + saves/restores collapseStateCache (change_id-keyed). startEdit/discardFile guard against navigation-during-await by comparing captured changeId vs LIVE diffTarget prop. `quickResolve(path, 'ours'|'theirs')` = one-click whole-file conflict resolve: reuses `fetchFileForEdit` (auto-`jj edit` non-`@`) + `reconstructSides` then writes the WHOLE side (sides.ours/theirs) via `api.fileWrite` — NOT planTake's incremental surgery, so it sidesteps the planTake blank-line gap; N-way/git-style/auto-resolved-race falls back to the editor (mirrors startMerge); an EMPTY chosen side (modify/delete — jj materializes the deleted side as empty) is refused with an editError instead of written, since fileWrite has no delete and a zero-byte write would mis-resolve the tree. `toggleSplitView()` is EXPORTED and is the only sanctioned way to flip split/unified from outside (App's Cmd+K palette routes through it) — it confirms before discarding an open FileEditor buffer; writing `config.splitView` directly bypasses that confirm. `[`/`]` → `stepHunk(dir)` over `flatHunks` (cross-file); `{`/`}` → `stepAnnotation(dir)` over `navAnnotations` (file-order sorted). Both via `scrollToHunk(path, hunkIdx)` (revealFile → rAF → data-attr query).
- **`diff-cache.ts`** — App-lifetime caches: derivedCache (highlight+word-diff, commit_id-keyed), parsedDiffCache (diff-text-keyed for ref-identity on revisit), collapseStateCache (change_id-keyed `CollapseMemo` = the user's explicit `{collapsed, expanded}` intent sets — the auto-collapse predicate is re-evaluated live, never persisted). Previously DiffPanel `<script module>`; hoisted so clearDiffCaches() is callable from hard-refresh. NOT wired into clearAllCaches() (would create api.ts ↔ diff-cache cycle); App calls both.
- **`FileSelectionPanel.svelte`** — Squash/split/review file checkbox panel. j/k/Space/a/n keys, auto-focus on mount. Mounted via `{#if fileSelectionMode}` so mount = mode entry.
- **`RevisionHeader.svelte`** — Header slot rendered by DiffPanel via `{@render header()}`: change_id, description expand, bookmark/PR badges, Describe/Divergence buttons, DescriptionEditor
- **`DiffFileView.svelte`** — Individual file diff with collapsible sections, context expansion, conflict A/B badges, Alt+click quick-annotate (uniform with markdown preview). Conflict header has one-click `◀ Ours`/`Theirs ▶` (`onresolveconflict`) beside `⧉ Merge` — gated on `!editing` (a resolve bumps commit_id not change_id, so the reset effect won't fire; an open editor's stale buffer would Save back over the resolution) + `disabled={editBusy}`. `bodyDeferred` prop = body not yet mounted: renders an estimated-height `.diff-body-placeholder` (height from `bodyEstPx` — same estimate that feeds the per-file inline `contain-intrinsic-size` hint) instead of line DOM; header/file-notes/composer still render, and the parent guarantees previewing/editing files are never deferred. Anything that needs a file's body DOM must go through DiffPanel's `revealFile()` first.
- **`FileEditor.svelte`** — CodeMirror 6 wrapper for inline editing (split-view right column)
- **`MergePanel.svelte`** — 3-pane conflict editor (ours | result | theirs). 3 raw CM6 EditorViews — no @codemirror/merge dep. Position surgery (takeBlock separator math, blockTracker mapPos + inversion) EXTRACTED to merge-surgery.ts. applyBlock/editInside/restoreBlock StateEffects + invertedEffects undo inverse — ALL THREE (including restoreBlock itself, so undo→redo restores source tag too). Center seeded with theirs. Reads `sides.blocks` from conflict-extract (no LCS — parse already knows region boundaries). Block nav: `[`/`]` keys + "N of M" pill + amber current-block ring; `currentBlockIdx` NOT derived from scrollTop (explicit nav is predictable when multiple blocks fit on screen). `[`/`]` gated on `!centerEl.contains(target)` — brackets are valid source chars. Minimap: 12px right-edge strip, chips positioned from immutable theirs-lines (conflict positions don't change, only color), click-to-scroll.
- **`merge-surgery.ts`** — PURE — planTake() = takeBlock's separator-math. Branch ORDER is load-bearing: srcEmpty before from===to (empty line tracks as zero-width; previous order made the srcEmpty deletion unreachable → silent no-op). Empty-line zero-width disambiguated by oppEmpty (opposite-side block range: false=empty-line IS block content, replace; true=shared LCS content, insert-before with trailing \n). planTakeBoth() = concatenate ours+'\n'+theirs for additive conflicts — simpler than planTake (both-sides-nonempty precondition means no separator-math). remapBlock() = mapPos with whole-block-replace inversion. initialTrackPos() = 1-indexed line → 0-indexed char. BlockSource = 'ours'|'theirs'|'mixed'|'both'. merge-surgery.test.ts: single-block round-trip (ours→theirs→identity) + fixture-driven full-pipeline (reconstructSides' sides.blocks → planTake).
- **`cm-shared.ts`** — CM6 helpers shared by FileEditor + MergePanel: detectIndent (tab/space + width, 200-line sample, >8sp ignored as alignment), getCmLanguage (async; RICH_SUPPORT for js/ts/py/go/rust, registry-derived bare LRLanguage/StreamLanguage fallback for everything else — every LANGUAGES entry highlights in-editor), cmTheme (CSS-var-driven, 18px line height matches diff rows)
- **`conflict-extract.ts`** — reconstructSides(): parses jj-native conflict markers (Diff/Snapshot styles, {7,} length — jj escalates) into {base, ours, theirs, blocks}. `blocks: ChangeBlock[]` = one per <<<<<<< / >>>>>>> region, recorded at parse time — replaces MergePanel's diffBlocks LCS call (the inRegion state machine already knows the boundaries; LCS was re-discovering that out-of-region lines are identical). Git-style markers + 3+ sides → null (fall through to raw FileEditor). State machine over < % + - > markers; % section uses ` `/`-`/`+` single-char prefixes per line.
- **`merge-diff.ts`** — ChangeBlock/LineDiff types + blocksToLineSets(). diffBlocks() (line-level LCS) is TEST-ONLY — conflict-extract.ts builds sides.blocks at parse time; the LCS stays as a fixture generator for merge-surgery/MergePanel tests.
- **`ConflictQueue.svelte`** — Merge-mode left rail: flattens `ConflictEntry[]` (from `GET /api/conflicts`) into a navigable file list, grouped by commit with changeId headers. Entries reversed to earliest-first so select(0) lands on a propagation root; later occurrences of the same path dimmed with ↑ hint (jj's resolve-earliest guidance). j/k clamp (no wrap — unlike MergePanel [/] which wraps; queue is a linear worklist), ○/● resolved dots from `Set<commitId:path>`, N-way red badge when sides>2. Exports handleKeydown for App delegation (BookmarksPanel pattern). JS-tracked `hoveredIdx` via delegated `onmousemove` + `data-idx` (same anti-:hover pattern as RevisionGraph). `scrollTo(i)` queries `[data-idx="${i}"]` NOT `.cq-selected` — the `$state` write hasn't re-rendered when called synchronously; static attrs are valid immediately.
- **`DocView.svelte`** — Doc-mode ProseMirror editor (View|Edit). Owns EditorView+EditorState; session owns Node + comments. `class="… prose"` for typography parity with MarkdownPreview. decoSet $derived (comments → Decoration.inline) pushed via setProps; `focusedComment` prop adds .focused ring. nodeViews from pm-mermaid.ts. applyReplace/scrollTo exported for rail Accept/jump.
- **`DocCommentRail.svelte`** — Doc-mode comment rail. Thin wrapper: `threads` $derived (parentId grouping, sorted by `from`) → `<CommentCard review={fromDocComment(root)} replies anchorText={selection}>`; callbacks wire to `session.resolveComment/addComment/removeComment`. `vis?.hiddenAuthors` filters with chip-restore in header. `stepComment(dir)` exported for `{`/`}` (wraps over open visibleThreads, calls onjump+onhover).
- **`review.ts`** — Unified read-model over Annotation + DocComment ([design-notes/unified-review.md](design-notes/unified-review.md)). `Review` + discriminated `ReviewAnchor` (diff|prose) + `PlacedReview` (adds derived orphaned/line/from/to). READ-ONLY adapters `fromAnnotation`/`fromDocComment` — no inverse; mutations go through existing stores by id. `anchorText(r)` + `SEVERITY_VAR` for CommentCard.
- **`CommentCard.svelte`** — Pure presentational comment card extracted from DocCommentRail's `.thread` block. Props: `{review, anchorText, staleness?, replies, orphaned}` + intent callbacks (`onresolve/onreply/onaccept/ondelete/onhideauthor/onjump/onhover`). Never imports ReviewAnchor — surface computes anchorText/staleness. 3px `--sev` left-border, ⟐ agent prefix, suggestion del/ins preview, Resolve/Won't-fix split. `relativeTime` (not local fmtAge).
- **`comment-visibility.svelte.ts`** — `createCommentVisibility()` per-App-instance factory (instantiated in App.svelte alongside `nav`, passed to DiffPanel as prop — DiffPanel unmounts on view-switch). Tri-state `mode: auto|hide|show` (⇧C cycles, clears overrides) + `overrides: SvelteMap<id,bool>` (true=force-visible, false=force-bubbled). `renderState(r, {hasDraft, hasVisibleReplies}) → visible|bubbled|stub|hidden` resolution order: draft-pin > hiddenAuthors > override > mode. `toggleThread(id, want)` — `want` REQUIRED (caller passes `!isVisible(r)`; store can't derive from id alone since auto-mode bubbling depends on r.resolution). `hiddenAuthors` reads `config.hiddenCommentAuthors`. `scrollGen` for holdViewport coordination.
- **`doc-session.svelte.ts`** — createDocSession(filePath, getWCId) factory. Two-tier model (in-memory PM ↔ file via commitBack with content-hash OCC). buildTextMap walks text nodes → flat text + bidirectional offset maps; **toPM(off, bias)** — bias=1 (from)→next-seg start, bias=-1 (to)→prev-seg end, or insertText spans node boundaries (table cells, para→heading). tm.place(hit) applies the bias pair (zero-width hits use one bias). refreshComments() (4s poll) preserves local from/to for known ids with unchanged anchor (onTransaction remap is exact; refind would orphan accepted suggestions). gen + inFlightMutation guards. orphanedComments filters !resolution.
- **`pm-schema.ts`** — ProseMirror Schema + parseMarkdown (marked Lexer→PM) + serializeMarkdown. Nodes: doc/paragraph/heading/blockquote/code_block/hr/passthrough/lists/hard_break/image/table; marks: em/strong/code/del/link. SAFE_LINK_SCHEME gates href/src in toDOM. escapeInline escapes md syntax position-sensitively (intraword _ left bare, line-start #/> escaped). closeDelim[] tracks code-mark backtick count at open.
- **`pm-mermaid.ts`** — code_block[lang=mermaid] NodeView: renders via ensureMermaidLoaded/tryRenderDiagram (same pipeline as MarkdownPreview), per-block ⟨⟩ source / ◇ diagram toggle. contentDOM (`<pre><code>`) stays mounted (CSS-hidden) so PM editing path is undisturbed; ignoreMutation excludes the SVG host. Non-mermaid → trivial pre/code NodeView.
- **`reanchor.ts`** — Content-addressed anchor model: captureAnchor(text, from, to) + refind(anchor, text). 3-stage: exact → normalized fallback (normalizeWithMap strips MD_SYNTAX + collapses whitespace with index map back to original; normSel trimmed so "# H"→" H" still matches) → zero-width context-only. findIn: tied contextScore → 'ambiguous' (don't silently pick first).
- **`FileHistoryPanel.svelte`** — Kaleidoscope-style two-cursor file history overlay. `api.fileHistory(path)` (= `LogGraph` with `files(root-file:"path")` revset) feeds the left rail; `api.diffRange(A, B, [path])` via createLoader feeds the right diff. `cursorB` (j/k) vs `pinnedA` (Space) — both default to 0 → empty-state prompt on mount. Full-screen overlay; `{#key fileHistoryPath}` remount = fresh cursors per path. Added to `anyModalOpen` (blocks log j/k beneath); keyboard delegated BEFORE the modal gate so overlay's own keys work. Entry: DiffPanel right-click → "View history" → `onfilehistory` callback prop.
- **`DescriptionEditor.svelte`** — Inline commit message editor
- **`CommandPalette.svelte`** — Fuzzy-search command palette (Cmd+K). `children?: PaletteCommand[]` = submenu (drill-in via breadcrumb, Esc backs out); `swatch?: string[]` renders 4-chip color strip. Submenu state stores LABEL not the cmd object — the object is fresh each `commands` recompute, storing it would freeze `children` at drill-in time and miss lazy-loaded additions.
- **`ContextMenu.svelte`** — Reusable right-click context menu (positioned at cursor)
- **`StatusBar.svelte`** — Bottom status bar with mode indicators and shortcuts
- **`MessageBar.svelte`** — Single user-facing message surface (error/warning/success). Fixed overlay above StatusBar (24px). Optional `details` (multi-line jj output → [+N] expand) + optional `action` button (replaces ✕ dismiss — e.g. "Update stale"). Exports `Message` type + `errorMessage()` coercer. Layered `linear-gradient(--msg-bg)` over opaque `--base` — the `--bg-*` tints are ~0.1 alpha (designed for inline use), bleed through panel footers as a fixed overlay.
- **`TabBar.svelte`** — Tab strip (above toolbar): click to switch, ✕ close, + to open. Dropdown uses workspace paths from /api/workspaces.
- **`BookmarkModal.svelte`** — Bookmark modal: noun-first list, Enter=move, a=advance (forward-only, jj refuses backwards — no confirm), d/f/t action keys with double-press confirm (via confirm-gate). Modal (not input) holds focus on open — `tick().then(() => modalEl?.focus())` since `{#if open}` hasn't mounted when `$effect` fires.
- **`BookmarksPanel.svelte`** — Branches view (activeView='branches'): renders in the RIGHT column beside RevisionGraph (not full-width). Flat list sorted trouble-first (conflict→diverged→ahead→…→synced). Sync dot + PR badge + sync label + commit description + age. `graphCommitId` prop → rows whose jumpTarget matches the graph cursor get `.bp-row-matches-graph` (amber tint + right-border, mirrors graph's amber left-border). handleKeydown EXPORTED — App delegates via bind:this so panel owns d/f/t/r regardless of DOM focus. Right-click menu: `oncontextmenu` passes `(bm, actions, x, y)` where `actions: BookmarkRowActions` are computed panel-side via `computeActions(bm)` (same helper d/f/t keys use); right-click disarms pending confirm + syncs keyboard index.
- **`bookmark-sync.ts`** — classifyBookmark(bm, scopeRemote?) → 8 sync states (incl. `secondary`: default-remote synced but a secondary tracked remote is off — amber, priority 4). scopeRemote for per-remote-group rows — must gate r.tracked or the 0/0 sentinel falls through to synced (false green dot). Scoped skips bm.synced all-remotes check (that's LOCAL-group concern). syncPriority() trouble-first sort, fmtCount() compact formatting (7.4k, 131k)
- **`remote-visibility.ts`** — buildVisibilityRevset(vis, bookmarks) → revset string. Per-bookmark enumeration emits `"name"@"remote"` NOT `"name@remote"` — the @ must stay UNQUOTED (it's the AT operator, not part of a string literal). revsetQuote() escapes \\ then " (order matters). Empty-hidden uses remote_bookmarks() shorthand; all-hidden contributes no part (avoids `ancestors(, 2)` syntax error).
- **`themes.ts`** — THEMES (7 builtins, CSS-baked for zero-FOUC) + loadGhosttyThemes() lazy-import of ghostty-themes.json (486 raw {bg,fg,p[16]}, ~35KB gzip, frontend/scripts/build-themes.mjs generates from Ghostty.app or $GHOSTTY_THEMES). deriveTheme() = single ANSI→lightjj-primary mapping (pickAccent() lum-guard falls back p[N]→p[N+8]→hardcoded; surface2=p[8] not mix(bg,fg); pickGraphPalette() ranks ANSI hues by colorDist from amber/green/red then mutes 25% toward bg — fixes graph-0==amber for ghostty themes). applyGhosttyTheme() `<style>` injection via textContent (NOT innerHTML — load-bearing vs `</style><script>` breakout). .catch clears memo (CLAUDE.md sync.Once anti-pattern). isThemeDark reads non-reactive ghosttyLoaded — callers must add `void ghosttyThemes` dep if wrapping in $derived.
- **`jj-features.svelte.ts`** — Per-feature jj version gates. JJ_FEATURES table + reactive jjSupports()/missingJJFeatures(); detected version set once by App.loadInfo. OPTIMISTIC on unknown (don't hide UI from dev builds) — backend's parallel jjSupports is PESSIMISTIC (avoid 500 on unsupported template).
- **`confirm-gate.svelte.ts`** — `createConfirmGate<K>()` double-press factory: first press arms, second of SAME key fires, any other key disarms. No timeout — nav disarms naturally. Shared by BookmarkModal + BookmarksPanel.
- **`BookmarkInput.svelte`** — Bookmark name input with autocomplete
- **`ConfigModal.svelte`** — Cmd+K → "Edit config" CodeMirror JSON editor. Raw `fetch('/api/config')` (non-jj endpoint, same rationale as config.svelte.ts), parse-validate on save, POST then `config.applyPartial(parsed)` for live-apply. FileEditor lazy-imported.
- **`GitModal.svelte`** — Git push/fetch modal. `data` createLoader fetches [bookmarks, remotes]; `selectedRemote = $derived(remoteOverride ?? remotes[0])` — default tracks data, user override is separate nullable state (no post-load side-effect).
- **`EvologPanel.svelte`** — Evolution log: entry list with inline diffs (server emits rebase-safe inter_diff per entry), ArrowUp/Down navigation
- **`OplogPanel.svelte`** — Operation log panel
- **`DivergencePanel.svelte`** — Stack-aware divergence resolution. classify() → columns (one per /N version, rows = stack levels). `nVersions` is MAX(level widths) with template optional-chaining — arity-mismatch (!alignable) renders "—" placeholders instead of crashing on undefined.commit_id. refinedKind/crossColumnMerge from divergence-refined.ts; buildKeepPlan() from divergence.ts; strategy card via recommend() from divergence-strategy.ts; all executed in App.svelte. fileUnion fetched from ALL levels (`versions.flat()`, not tip-only — stacked non-tip edits would otherwise false-positive `pure-rebase`). `crossDiff` is a createLoader (loader.error rendered inline, separate from panel `error` so cross-diff failure doesn't hide Keep buttons). Immutable-sibling case: split-identity card instead of error string. {#key changeId} in parent enforces single-mount-per-changeId.
- **`divergence.ts`** — classify(): stack grouping (parent-change_id walk + alignColumns commit_id permutation), kind (same/diff/compound), liveVersion with jj-edit tautology guard, descendants, conflicted bookmarks. refineRebaseKind() fileUnion subtraction. buildKeepPlan(group, keeperIdx) → KeepPlan: abandon ALL levels of losing columns, bookmark repoints from ALL loser-column `v.bookmarks` (not just conflicted-only — a count=1 bookmark on a loser would cascade to trunk via --retain-bookmarks) to SAME-LEVEL keeper (not tip), keeper-COLUMN (not just tip) descendants excluded, nonEmptyDescendants carry per-descendant `rebaseTarget` (keeper at parent's level via commitIdToLevel map; MAX level for merges). See [jj-divergence.md](jj-divergence.md). divergence.test.ts has a generative invariant sweep (all shape combos × all keeperIdx values, 8 invariants).
- **`divergence-refined.ts`** — PURE — computeRefinedKind(group, diffLoading, crossDiffFiles, fileUnion) → RefinedKind (full taxonomy decision tree extracted from DivergencePanel $derived). findCrossColumnMerge(group) detects `jj new keeper loser` manual reconciliations (tip-level only — documented gap). Full truth-table coverage in divergence-refined.test.ts.
- **`divergence-strategy.ts`** — recommend(group, refinedKind) → ranked Strategy[] (keep/squash + confidence + reason). Mirrors jj-guide Strategies 1+3. Conservative: 'high' only for pure-rebase+live-hint. Squash gated on non-stack (tip-only resolution leaves intermediates divergent). Immutable-sibling: hardcoded buttons in panel, not Strategy[] — split-identity (metaedit --update-change-id) vs abandon-mutable.
- **`diff-parser.ts`** — Unified diff parser
- **`context-expand.ts`** — PURE — expandGaps(original, fullContext, revealedSet) → {file, gapMap}. Merges adjacent hunks when the gap between them is revealed; gapMap tracks effective→original index shift so subsequent gap clicks resolve correctly. DiffPanel tracks revealedGaps per path, lazy-fetches --context 10000 on first reveal.
- **`conflict-parser.ts`** — jj conflict marker parser; diff-side labels use \\\\\\\ "to:" value (what :ours keeps), not %%%%%%% "from:"
- **`split-view.ts`** — Side-by-side diff alignment
- **`word-diff.ts`** — Word-level inline diff computation
- **`perf-fixtures.ts`** — Deterministic synthetic diff/source generators (realistic, parseable TS/Go) for the perf bench suite. No randomness so runs are comparable across optimization passes.
- **`diff-compute.bench.ts`** — `pnpm run bench` (vitest bench): parse → Lezer highlight → word-diff LCS → split-view at threshold-anchored sizes. Compute only — a jsdom DiffFileView-mount bench was tried and dropped (no layout/paint → false confidence); DOM-side numbers come from the real-browser procedure (frontend/scripts/gen-bench-repo.mjs controlled repo) documented with the baselines in docs/design-notes/diff-perf-benchmarks.md.
- **`languages.ts`** — SINGLE language registry. One LANGUAGES entry per lang (`{exts, parser?|legacy?}`); EXTENSION_LANGUAGES/PARSERS/detectLanguage/needsLegacyParser/ensureLegacyParsers all derived. Main-bundle safe: only @lezer/* runtime imports + type-only @codemirror/language; legacy modes lazy-loaded. Adding a language = one table entry here.
- **`highlighter.ts`** — highlightLines (Lezer highlightCode → tok-* spans, sync, theme-independent class names) + escapeHtml/escapeAttr. Reads PARSERS from languages.ts.
- **`markdown-render.ts`** — marked (GFM) + DOMPurify. `renderMarkdown(src, ctx?, stampSrc?)` is sync; per-render mutable state (pendingDiagrams/imgCtx/footnotes/fnLabelToIdx/headingSlugs/stamp) bundled into ONE `rs: RenderState` slot — hooks read `rs!.x` (non-null inside the try/finally). `fnScope` stays module-level (cross-render counter). `stampedBlocks(container, totalLines)` → `{el, start, end}[]` for MarkdownPreview's gutter measurement.
- **`mermaid.ts`** — `ensureMermaidLoaded`/`tryRenderDiagram`/`wirePanzoom` — beautiful-mermaid lazy-load + render. Extracted from markdown-render so pm-mermaid.ts (doc-mode) imports the renderer without pulling marked + DOMPurify into its chunk.
- **`panzoom.ts`** — `wireSvg(svg, canvas)` hand-rolled wheel-zoom/drag-pan/dblclick-reset. Extracted so the .excalidraw lazy chunk doesn't pull marked/dompurify.
- **`excalidraw-render.ts`** — PURE — `renderExcalidrawSVG(jsonText)` → SVG string | null. Zero-dep clean-geometry renderer (no rough.js). rectangle/ellipse/diamond/line/arrow/text/freedraw; placeholder boxes for image/frame/embeddable. `escapeAttr` (NOT escapeHtml — `"` breaks out of attribute context) on color/viewBackgroundColor.
- **`ExcalidrawPreview.svelte`** — .excalidraw preview (lazy chunk, ~2KB gzip). `{@html svg}` + `wireSvg` panzoom; parse-error fallback.
- **`MarkdownPreview.svelte`** — .md preview toggle in DiffFileView header. `mermaidReady` dep gated on `hasMermaid` (skip re-parse for non-mermaid docs). Explicit `.md-gutter` column split into geometry+data: `blockGeometry` $state (post-render $effect, deps html+resizeEpoch only — DOM measurement) → `gutterRows` $derived (pure, deps geometry+forLine+addedLines) → `{#each}` strip+badge. `annotationsForLine` is the store's stable `forLine(filePath, lineNum)` — fresh closure per render would defeat the $derived short-circuit. ResizeObserver bumps `resizeEpoch`. Alt+click delegated via `onclick` on `.md-content`.
- **`fuzzy.ts`** — Fuzzy string matching
- **`group-by.ts`** — groupByWithIndex utility for per-file match scoping
- **`loader.svelte.ts`** — createLoader() async factory with generation counter
- **`revision-navigator.svelte.ts`** — createRevisionNavigator(): diff/files/description loaders + revGen batch-fetch orchestration + navigateCached (double-rAF paint-first deferral) / navigateDeferred (50ms debounce coalesce). cancel() clears both timers + bumps revGen — switchToLogView/onDestroy/handleRevsetSubmit all call it. **Sole owner of `loadedTarget`/`diffContentKey`/`diff.value` — every diff load goes through `loadDiffAndFiles`/`applyCacheHit`/`loadMulti` so the triple advances atomically.** App calling `diff.load()` directly desyncs DiffPanel's `contentMatchesTarget` (the v1.19→1.20 memo-poisoning class). Scheduling was previously inline in App.svelte's selectRevision; moved here so the "rapid j/k only renders destination" contract is testable with fake timers.
- **`diff-derivation.svelte.ts`** — createDiffDerivation() factory for per-file progressive computation (highlights, word-diff)
- **`keyboard-gate.ts`** — PURE — routeKeydown(ctx, handlers) gate-priority router extracted from App.svelte's handleKeydown. Tests lock the ORDER invariants (globalOverrides before inInput, inlineNav swallows all, etc).
- **`modes.svelte.ts`** — Rebase/squash/split mode state factories. `ModeBase.diffFollows` is the per-mode nav semantic (rebase=true destination-preview, squash/split=false frozen-on-source) — App derives `diffFrozen` from this so mouse onselect + the multi-check $effect read one source instead of each spelling out `squash.active || split.active`. `inlineMode` stays for binary gates; `diffFrozen` is the answer `inlineMode` can't give. SplitMode.review distinguishes 'v' (review: accepted/rejected labels) from 's' (split: stays/moves labels) — same jj split underneath.
- **`config.svelte.ts`** — Reactive config singleton — primary storage os.UserConfigDir()/lightjj/config.json, localStorage as write-through cache. `applyPartial()` pushes a parsed object into reactive state (known keys only) for ConfigModal live-apply; the save-effect then persists. `fontSize` getter clamps to `FONT_SIZE_MIN..MAX` with `Number()`+`isFinite` so all read sites (CSS var, palette label, ±1 arithmetic) see a sane value regardless of load path.
- **`recent-actions.svelte.ts`** — config-backed last-used timestamps for bookmarks (BookmarkModal/BookmarkInput recency sort)
- **`annotations.svelte.ts`** — Per-line review comment store for agent workflows. createAnnotationStore() + reanchor() + exportMarkdown/JSON. Server-side storage via /api/annotations (workspace tabs share). `side?: 'old'|'new'` (default new) lets comments target deleted lines; `byLine` key includes side. Re-anchor via diffRange delta + ±5 content scan (new-side only — old-side pinned to pre-image).
- **`AnnotationBubble.svelte`** — Annotation create/edit popup (severity select + textarea)
- **`WelcomeModal.svelte`** — "What's new" modal shown on version bump; content from tutorial-content.ts
- **`tutorial-content.ts`** — Feature announcements keyed by version
- **`version.ts`** — APP_VERSION constant (bump to show WelcomeModal)
