# pnpm 11 no longer reads the "pnpm" field in package.json; these settings
# live here now. See CLAUDE.md "Dependencies": onlyBuiltDependencies is the
# supply-chain allowlist (only esbuild may run install scripts; all else blocked).
onlyBuiltDependencies:
  - esbuild
patchedDependencies:
  beautiful-mermaid@1.1.3: patches/beautiful-mermaid@1.1.3.patch
